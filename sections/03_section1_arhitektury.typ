#import "../templates/style.typ": *

== Результаты исследования

=== Принцип работы CNN

Сверточная нейронная сеть представляет собой последовательность слоёв, каждый из которых преобразует входной массив данных (тензор) в новый признаковый тензор. Типовая структура CNN, предложенная ещё в архитектуре LeNet #cite-num(2), включает чередование сверточных слоёв, слоёв подвыборки (pooling) и завершающих полносвязных слоёв с функцией softmax на выходе #cite-num(1, 2).

*Операция свёртки и карты признаков.* Сверточный слой выполняет скользящее по пространственным координатам изображения поэлементное умножение фрагмента входного тензора на матрицу весов (ядро свёртки, фильтр) с последующим суммированием произведений и добавлением смещения (bias). Результатом применения одного фильтра ко всему изображению является карта признаков (feature map), на которой высокие значения соответствуют участкам изображения, наиболее похожим на паттерн, который «умеет» находить данный фильтр. Поскольку один и тот же фильтр применяется ко всем участкам изображения (разделяемые веса), число обучаемых параметров сверточного слоя не зависит от размера входного изображения и оказывается на порядки меньше, чем в полносвязном слое аналогичной «широты» — именно это архитектурное решение и легло в основу масштабируемости CNN #cite-num(1). Как правило, в одном слое используется набор из нескольких десятков–сотен различных фильтров, что позволяет одновременно выделять множество разнородных признаков — от простых границ и перепадов яркости в первых слоях до сложных, семантически содержательных паттернов в глубоких слоях сети.

*Функции активации.* После свёртки к результату применяется нелинейная функция активации. Начиная с архитектуры AlexNet #cite-num(9) стандартом де-факто стала функция ReLU (rectified linear unit, $f(x) = max(0, x)$), которая, в отличие от сигмоиды и гиперболического тангенса, не насыщается при положительных значениях аргумента и позволяет существенно ускорить обучение глубоких сетей.

*Слои подвыборки (pooling).* Слои подвыборки уменьшают пространственную размерность карт признаков, обобщая значения в пределах локальной окрестности (чаще всего — выбором максимума, max-pooling). Это снижает вычислительную нагрузку в последующих слоях, увеличивает эффективное рецептивное поле нейронов и повышает устойчивость сети к небольшим сдвигам и искажениям входного изображения.

*Полносвязные слои и функция потерь.* Завершающие слои классической CNN — один или несколько полносвязных слоёв, преобразующих признаки, извлечённые сверточной частью сети, в итоговый вектор вероятностей классов (как правило, через функцию softmax). Обучение сети производится путём минимизации функции потерь (чаще всего — категориальной кросс-энтропии для задач классификации) с использованием алгоритма обратного распространения ошибки (backpropagation): градиент функции потерь последовательно распространяется от выходного слоя ко входному, а параметры сети обновляются одним из вариантов стохастического градиентного спуска.

*Переобучение и регуляризация.* Из-за большого числа параметров глубокие CNN подвержены переобучению — ситуации, при которой сеть запоминает обучающую выборку, но плохо обобщается на новые данные. Для борьбы с переобучением применяются: регуляризация весов (L1/L2), исключение случайных нейронов на этапе обучения (dropout, впервые применённый в AlexNet #cite-num(9)), нормализация по мини-батчам (batch normalization, использованная, в частности, в ResNet #cite-num(12)), а также искусственное расширение обучающей выборки (аугментация данных) и перенос обучения с предварительно обученных моделей #cite-num(1, 3).

=== Основные архитектуры CNN

Развитие архитектур CNN на протяжении последних трёх десятилетий шло по пути постепенного увеличения глубины сети и одновременного поиска способов сделать обучение всё более глубоких сетей возможным и вычислительно эффективным, а начиная с середины 2010-х годов — также по пути специализированного проектирования лёгких архитектур для мобильных и встраиваемых устройств #cite-num(1).

*LeNet* — одна из первых практически применённых сверточных архитектур, предложенная Я. ЛеКуном с соавторами для задачи распознавания рукописных цифр #cite-num(2). Сеть состоит из чередующихся сверточных слоёв и слоёв подвыборки с последующими полносвязными слоями; несмотря на скромную по современным меркам глубину, именно в этой работе были заложены базовые принципы построения CNN, используемые во всех последующих архитектурах.

*AlexNet* #cite-num(9) — архитектура, победившая в конкурсе ImageNet Large Scale Visual Recognition Challenge (ILSVRC) 2012 года с результатом top-5 ошибки 15,3 % против 26,2 % у второго места, что стало точкой отсчёта современной эпохи глубокого обучения в компьютерном зрении. Сеть состоит из пяти сверточных слоёв и трёх полносвязных слоёв, содержит порядка 60 млн обучаемых параметров и обучалась с использованием функции активации ReLU, dropout-регуляризации и распределённых вычислений на графических процессорах.

*VGG* #cite-num(10) — семейство архитектур (VGG-11…VGG-19), исследовавшее эффект увеличения глубины сети при использовании исключительно малых свёрточных фильтров размером 3×3. Сети VGG заняли первое и второе места в задачах локализации и классификации ILSVRC 2014 соответственно и продемонстрировали, что последовательное увеличение глубины до 16–19 весовых слоёв при простой, единообразной структуре само по себе даёт существенный прирост точности.

*GoogLeNet / Inception* #cite-num(11) — архитектура глубиной 22 слоя, победившая в конкурсе ILSVRC 2014. Ключевая идея — модуль Inception, в котором свёртки разного размера (1×1, 3×3, 5×5) и операция подвыборки применяются параллельно и объединяются, что позволяет более эффективно использовать вычислительные ресурсы при увеличении глубины и ширины сети без пропорционального роста вычислительных затрат.

*ResNet* #cite-num(12) — архитектура с остаточными связями (residual connections), при которых каждый блок обучается предсказывать не само выходное представление, а лишь остаточную поправку к входному сигналу. Такое решение устранило проблему затухания градиента при увеличении глубины сети и впервые сделало возможным устойчивое обучение сетей глубиной в десятки и сотни слоёв.

*DenseNet* #cite-num(13) — развитие идеи остаточных связей, в котором каждый слой внутри плотного блока (dense block) получает на вход конкатенацию признаковых карт всех предыдущих слоёв блока. Авторы показали, что при $L$ слоях такая схема образует $L(L+1)/2$ прямых связей вместо $L$ связей в традиционной последовательной сети, что усиливает распространение признаков и градиента, поощряет их повторное использование и позволяет существенно сократить число параметров при сопоставимой или более высокой точности по сравнению с ResNet.

*MobileNet* #cite-num(14) — семейство архитектур, специально спроектированных для мобильных и встраиваемых устройств. В основе лежит разделяемая по глубине свёртка (depthwise separable convolution), раскладывающая обычную свёртку на два более дешёвых по вычислениям этапа, а также два глобальных гиперпараметра, позволяющих разработчику напрямую регулировать баланс между задержкой (latency) и точностью модели под конкретные ограничения задачи.

*EfficientNet* #cite-num(15) — семейство архитектур (EfficientNet-B0…B7), построенное на основе идеи составного масштабирования (compound scaling): глубина, ширина и разрешение входного изображения сети увеличиваются согласованно, по единому коэффициенту, а не по отдельности. Старшая модель семейства, EfficientNet-B7, достигла точности 84,4 % (top-1) / 97,1 % (top-5) на ImageNet, будучи при этом в 8,4 раза компактнее и в 6,1 раза быстрее на этапе вывода по сравнению с сопоставимой по точности предшествующей архитектурой.

#gost-table("Сравнение основных архитектур CNN")[
  #set text(size: 10pt)
  #table(
    columns: (0.9fr, 0.5fr, 1.3fr, 1.3fr, 1.3fr),
    align: (center, center, left, left, left),
    stroke: 0.5pt,
    table.header(
      [*Архитектура*], [*Год*], [*Ключевая идея*], [*Преимущества*], [*Недостатки / ограничения*],
    ),
    [LeNet #cite-num(2)], [1998],
      [Чередование свёртки и подвыборки; базовая схема CNN],
      [Простота, малые вычислительные затраты, историческая основа всех последующих CNN],
      [Ограниченная выразительная способность; неприменима для крупномасштабных задач],

    [AlexNet #cite-num(9)], [2012],
      [Глубокая сеть на GPU; ReLU; dropout],
      [Резкий скачок точности на ImageNet; заложила современную парадигму обучения CNN],
      [Большое число параметров (порядка 60 млн), значительная часть которых сосредоточена в полносвязных слоях],

    [VGG #cite-num(10)], [2014],
      [Глубина за счёт однородных свёрток 3×3],
      [Простая, регулярная структура; хорошая переносимость признаков на другие задачи],
      [Очень большое число параметров и высокая вычислительная стоимость по сравнению с более поздними архитектурами],

    [GoogLeNet / Inception #cite-num(11)], [2014],
      [Параллельные свёртки разного размера (Inception-модуль)],
      [Эффективное использование вычислительных ресурсов при увеличении глубины/ширины],
      [Более сложная и менее регулярная структура, усложняющая модификацию архитектуры],

    [ResNet #cite-num(12)], [2016],
      [Остаточные связи (residual connections)],
      [Устойчивое обучение очень глубоких сетей; высокая точность; широкое распространение как база для переноса обучения],
      [Рост глубины по-прежнему увеличивает вычислительные затраты и требования к памяти],

    [DenseNet #cite-num(13)], [2017],
      [Плотные связи между всеми слоями блока],
      [Эффективное повторное использование признаков; меньше параметров при сопоставимой точности],
      [Повышенный расход памяти на хранение конкатенированных карт признаков при обучении],

    [MobileNet #cite-num(14)], [2017],
      [Разделяемая по глубине свёртка],
      [Низкая вычислительная стоимость; пригодна для мобильных/встраиваемых устройств; настраиваемый баланс точность/скорость],
      [Как правило, уступает по точности крупным архитектурам на сложных задачах],

    [EfficientNet #cite-num(15)], [2019],
      [Согласованное (составное) масштабирование глубины, ширины и разрешения],
      [Рекордное соотношение точности и вычислительных затрат среди сетей своего поколения],
      [Поиск базовой архитектуры и коэффициентов масштабирования требует значительных вычислительных ресурсов на этапе проектирования],
  )
]

Из таблицы 1.1 видно, что развитие архитектур CNN не сводится к простому «наращиванию глубины»: после ResNet и DenseNet, показавших, что глубина сама по себе перестаёт быть узким местом благодаря остаточным и плотным связям, вектор развития сместился в сторону эффективности — MobileNet и EfficientNet предлагают не более точные, а более экономичные по вычислениям решения, зачастую жертвуя частью точности ради применимости на устройствах с ограниченными ресурсами #cite-num(1, 14, 15). Это наблюдение подтверждается и специализированными обзорами по отдельным прикладным областям, которые отдельно указывают на MobileNet и EfficientNet как на предпочтительные архитектуры для встраиваемых и мобильных приложений #cite-num(6).
